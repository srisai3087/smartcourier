-- SmartCourier - Docker Database Initialization
-- This runs automatically when MySQL container starts

CREATE DATABASE IF NOT EXISTS auth_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS delivery_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS tracking_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS admin_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Seed roles in auth_db
USE auth_db;
CREATE TABLE IF NOT EXISTS roles (id BIGINT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(50) UNIQUE);
INSERT IGNORE INTO roles (name) VALUES ('ROLE_CUSTOMER');
INSERT IGNORE INTO roles (name) VALUES ('ROLE_ADMIN');

-- Seed hubs in admin_db
USE admin_db;
CREATE TABLE IF NOT EXISTS hubs (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    hub_name VARCHAR(100),
    city VARCHAR(100),
    state VARCHAR(100),
    pin_code VARCHAR(10),
    is_active BOOLEAN DEFAULT TRUE
);
INSERT IGNORE INTO hubs (hub_name, city, state, pin_code, is_active) VALUES
  ('Mumbai Central Hub', 'Mumbai', 'Maharashtra', '400001', true),
  ('Delhi North Hub', 'Delhi', 'Delhi', '110001', true),
  ('Bangalore Tech Hub', 'Bangalore', 'Karnataka', '560001', true),
  ('Chennai South Hub', 'Chennai', 'Tamil Nadu', '600001', true),
  ('Kolkata East Hub', 'Kolkata', 'West Bengal', '700001', true);

SELECT 'Docker database initialization complete!' AS status;
